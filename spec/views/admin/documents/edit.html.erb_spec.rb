require 'spec_helper'

describe "admin/documents/edit" do
  # before(:each) do
  #   @admin_document = assign(:admin_document, stub_model(Admin::Document))
  # end

  # it "renders the edit admin_document form" do
  #   render

  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form[action=?][method=?]", admin_document_path(@admin_document), "post" do
  #   end
  # end
end
