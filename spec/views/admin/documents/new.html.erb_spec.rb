require 'spec_helper'

describe "admin/documents/new" do
  # before(:each) do
  #   assign(:admin_document, stub_model(Admin::Document).as_new_record)
  # end

  # it "renders new admin_document form" do
  #   render

  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   assert_select "form[action=?][method=?]", admin_documents_path, "post" do
  #   end
  # end
end
