require 'spec_helper'

describe "admin/documents/index" do
  # before(:each) do
  #   assign(:admin_documents, [
  #     # stub_model(Admin::Document),
  #     stub_model(Admin::Document)
  #   ])
  # end

  # it "renders a list of admin/documents" do
  #   render
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  # end
end
