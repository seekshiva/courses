<%= form_for([:admin, @department]) do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <legend><%= @legend %></legend>
    
    <div class="field">
      <%= f.label :short %>
      <%= f.text_field :short, class: "input-mini", placeholder: "Short" %>
    </div>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name, class: "input-xxlarge", placeholder: "Department Name" %>
    </div>
    <div class="field">
      <%= f.label :hod %>
      <%= f.text_field :hod, class: "input-xxlarge", placeholder: "Head of the Department" %>
    </div>
    <div class="field">
      <%= f.label :rollno_prefix %>
      <%= f.text_field :rollno_prefix, class: "input-mini",placeholder: "Prefix" %>
    </div>
    <div class="actions">
      <%= f.submit class: "btn btn-primary btn-large" %>
      <%= link_to "Back", admin_departments_path, class: "btn btn-small" %>
    </div>
  </fieldset>
<% end %>
