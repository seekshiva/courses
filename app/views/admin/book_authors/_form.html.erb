<%= form_for([:admin, @book_author]) do |f| %>
<% if @book_author.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@book_author.errors.count, "error") %> prohibited this book_author from being saved:</h2>
  
  <ul>
    <% @book_author.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<fieldset>
  <legend><%= @legend %></legend>
  <div class="field">
    <%= f.label :book_id %>
    <%= f.select :book_id, options_for_select(@books_array) %>
  </div>
  <div class="field">
    <%= f.label :author_id %>
    <%= f.select :author_id, options_for_select(@authors_array) %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-small btn-primary" %>
    <%= link_to 'Back', admin_book_authors_path, class: "btn btn-small" %>
  </div>
</fieldset>
<% end %>
