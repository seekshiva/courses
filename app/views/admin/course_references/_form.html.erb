<%= form_for([:admin, @course_reference]) do |f| %>
  <% if @course_reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_reference.errors.count, "error") %> prohibited this course_reference from being saved:</h2>

      <ul>
      <% @course_reference.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <fieldset>
    <legend><%= @legend %></legend>
    <div class="field">
      <%= f.label :course_id %><br />
      <%= f.select :course_id, options_for_select(@courses_array) %>
    </div>
    <div class="field">
      <%= f.label :book_id %><br />
      <%= f.select :book_id, options_for_select(@books_array) %>
    </div>
    <div class="actions">
      <%= f.submit class: "btn btn-small btn-primary"%>
      <%= link_to "Back", admin_course_references_path, class: "btn btn-default btn-small" %>
    </div>
  </fieldset>
<% end %>
