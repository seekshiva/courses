<%= form_for(@term_document) do |f| %>
  <% if @term_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@term_document.errors.count, "error") %> prohibited this term_document from being saved:</h2>

      <ul>
      <% @term_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
