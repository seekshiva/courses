<h1>Listing section_documents</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @section_documents.each do |section_document| %>
  <tr>
    <td><%= link_to 'Show', section_document %></td>
    <td><%= link_to 'Edit', edit_section_document_path(section_document) %></td>
    <td><%= link_to 'Destroy', section_document, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Section document', new_section_document_path %>
