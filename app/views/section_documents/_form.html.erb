<%= form_for(@section_document) do |f| %>
  <% if @section_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section_document.errors.count, "error") %> prohibited this section_document from being saved:</h2>

      <ul>
      <% @section_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
